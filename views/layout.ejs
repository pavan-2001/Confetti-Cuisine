<!DOCTYPE html>
<html>
    <head>
        <title>Confetti Cuisine</title>
        <meta char="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">

        <style>
            * {
                box-sizing : border-box;
                flex : 1;
            }
            body {
                font-family :Arial, Helvetica, sans-serif;
            }
            header {
                text-align : center;
                align-items: center;
                font-size: 100px;
                color : black;
                font-family:fantasy ;
            }
            header h1{
                font-size: 60px;
                font-family :-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
                border-color: azure;
                font-style:italic;
            }
            header button {
                width: 15%;
                background-color : black;
                color : white;
                border-radius: 10%;
                height: 35px;
                cursor: pointer;
                align-self:center;
                margin-left: 3%;
                margin-right: 3%;
                font-style: italic;
                font-family: cursive;
            }
        </style>
        <header>
            CONFETTI CUISINE
            <nav>
                <button type="button" onclick="location.href='http://localhost:3000'">Home</button>
                <button type="button" onclick="location.href='http://localhost:3000/courses'">Courses</button>
                <button type="button" onclick="location.href='http://localhost:3000/subscribers/new'">Contact</button>
                <button type="button" onclick="location.href='http://localhost:3000/users'">Users</button>
            </nav>
            <a href="/chat" class="chat-icon">@</a>
        </header>
        <ul><li><%- include('courses/_courseModal')%></li></ul>
    </head>
    <body>
        <div class='flashes'>
            <%if(flashMessages) {%>
                <%if(flashMessages.success) {%>
                    <div class='flash success'><%=flashMessages.success%></div>
                <%} else if(flashMessages.error) {%>
                    <div class='flash error'><%=flashMessages.error%></div>
                <%}%>
            <%}%>
        </div>
        <div>
            <% if(loggedIn) {%>
                <p>Logged in as <a href="<%=`/users/${currentUser._id}`%>"><%= currentUser.fullName %></a>
                    <a href="<%= `/users/logout`%>">Log out</a></p>
            <% } else { %>
                <a href="<%=`/users/login`%>">Log in</a>
            <% } %>
        </div>
        <div>
            <% if(loggedIn) {%>
                <div id="apiToken" data-token="<%= currentUser.apiToken%>" style="display : none;"></div>
            <%}%>
        </div>
        <%-body%>
        <script type=" text/javascript" src='js/bootstrap.min.js' ></script>
        <script type=" text/javascript" src='js/jQuery.min.js' ></script>
        <script src="/socket.io/socket.io.js" > </script>
        <script type="text/javascript" src='js/confettiCuisine.js' ></script>
    </body>
    
</html>